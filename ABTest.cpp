#include"A.h"
#include"B.h"

int main() {
    // to use class include its file
    B obj1{ 4 };// this is a constructor from class "A", if its not in class B -> error, use "uses" to include
    B obj2;
}
