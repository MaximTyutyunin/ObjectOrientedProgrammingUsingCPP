#include <iostream>  
#include <string>
#include <fstream>
#include <cstdlib> 
#include <bitset>// for binaries
using namespace std;



int main() {

    string s{ "c++" };
    string s{ 2, 'c' };
    string s = 'c';
    string s = "c++";


    //const int SIZE{ 3 };
    //SIZE++;
    //cout << SIZE;
    //cin >> SIZE;
    //x = SIZE--;


    /*int x1 , x2;
    x1 = x2 = 1;
    cout << bitset<8>(x1 & x2) << endl;
    cout << bitset<8>( ~(~x2)) << endl;
    cout << bitset<8>(x1 | x2) << endl;
    cout << bitset<8>( x1 ^ x2) << endl;*/

    /*int list[]{ 1,2,3,4 };
    cout << *(list + 1) << " " << *list + 1;*/

   /* ifstream inClientFile{ "filename",ios::in };

    if (!inClientFile) {
        cerr << "File could not be opened" << endl;
        exit(1);
    }
    int x, y;
    while (inClientFile >> x >> y) {
        cout << x << "/" << y << x / y << endl;
    }*/
        



}
